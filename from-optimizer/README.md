# Оптимизатор: сводка и инструкция (обновляется)

Отчёты по времени: `from-optimizer/<дата-время>.md`. Этот файл — текущее состояние.

## Ветки
- `opt/bucket-table` (от `core` @ 2bb90d5) — все готовые оптимизации, тесты зелёные (227 passed, 3 skipped):
  1. bbaf9a9 — таблицы корзин (индексатор рук Waugh + по байту на класс), включаются переменной окружения;
  2. 357f275 — динамическая раздача итераций по потокам (MCCFR и RNR);
  3. 8a18c43 — обход MCCFR по дереву историй торгов (однопоточно бит в бит с Python);
  4. 7c7074d — `scripts/bench_opt.py`, замер по потокам;
  5. dc9ed45 — RNR тоже по дереву историй;
  6. d75dcaa, 447b105 — память: 321 → 142 байта на инфосет (подробности в `2026-09-25_1430_memory.md`);
  7. 17edad1 — спинлок уступает квант, 16 потоков на 4 ядрах 11.4 → 20.1 M узлов/с (`2026-09-25_1530_threads.md`).
- `core`, `main`/`master` не тронуты.

## Как проверить на ПК (порядок)
Нужен **Python 3.12+**: `py_sum` в C++ повторяет компенсированный `sum()` из 3.12, на 3.11 побитовый тест падает.
```
git fetch origin
git checkout opt/bucket-table
python scripts/build_fast.py
python -m pytest -q                                   # ожидается: всё зелёное
python scripts/build_bucket_table.py --buckets data/buckets_<tag>.json --threads 16
    # -> data/bucket_tables/buckets_<kind>_<n>_<fingerprint>.npbt (~138 МБ),
    #    сверка с bucket() на 20 тыс. рук на улицу, при расхождении файл не пишется
set NEGPLURIBUS_BUCKET_TABLES=data\bucket_tables      # PowerShell: $env:NEGPLURIBUS_BUCKET_TABLES="data\bucket_tables"
python scripts/bench_opt.py --buckets data/buckets_<tag>.json --grid wide --threads 1,2,4,8,16
```
Для сравнения «до»: тот же `bench_opt.py` на ветке `core`. Скрипт `bench_opt.py` есть только в `opt/bucket-table`,
его можно скопировать в `core` без изменений. Сравнивать по **M nodes/s** на одинаковых `--warmup/--iters`.
Узлов на итерацию должно быть столько же, это проверка, что работа та же.

## Что меняется для пользователя обучения
- Без переменной `NEGPLURIBUS_BUCKET_TABLES` всё как раньше, плюс ускорение от дерева историй.
- С переменной `core_bucketer()` сам находит таблицу своего бакетера по fingerprint. Если таблицы нет, работает без неё.
  На пути к таблице не должно быть кириллицы (путь открывается как UTF-8 → wide API на Windows, но лучше не проверять).
- Форматы blueprint, чекпоинтов и корзин **не менялись**, identity бакетера та же.
- Многопоточно набор конкретных раздач теперь зависит от планирования потоков (распределение то же).
  Однопоточный режим детерминирован и равен Python.

## Замеры в облаке (4-ядерный Xeon, общий L3 260 МБ, шум ±10–30 %; с ПК не сравнимы)
HU 100bb, 4 улицы, E[HS] 8, 200 тыс. итераций после 200 тыс. прогрева:
узкая сетка 1 поток ×8.1, 4 потока ×7.0; широкая 1 поток ×5.1, 4 потока ×4.6. Подробности в `2026-09-25_1130_history-tree.md`.

## План (обновлено 25.09, 20:15)
Ждём:
1. Поиск (`search.h`, после части 3): дерево подыгры один раз на поиск, ранги вскрытий таблицей, корзины из таблиц (`2026-09-25_1800_search-profile.md`).
2. Замер на ПК: `bench_opt.py --threads 1,2,4,8,12,16`, когда машина свободна.

Возможные задачи, **ждут обсуждения пользователя с сенсеем и основной** (ничего не начато):
3. Флоп и тёрн таблицы potential быстрее в 2–3 раза, числа те же (таблица рангов на борд, RNG воспроизводится точно).
4. Сохранять признаки potential (гистограммы) один раз, чтобы таблица под любое число корзин (16/64/256) строилась за секунды. Числа те же.
5. Прунинг на 4 улицах: замер дуэлью. Меняет обучение, решение за пользователем и основной.
6. Расписание Linear CFR (всегда / первые N итераций, как в Pluribus / обычный CFR): замер по кривой пуш/фолда. Меняет обучение.
7. Точный E[HS] на ривере вместо Монте-Карло: меняет корзины E[HS]. Для боевых potential не нужно, у них ривер уже точный.
8. Видеокарта: пакетный обход (позже).

## Вопросы к сенсею / основной сессии
1. Какие корзины и параметры в боевых прогонах сейчас (kind, n_buckets, samples, bins)?
2. Результаты `bench_opt.py` на ПК по потокам 1/2/4/8/16, с таблицей и без.
