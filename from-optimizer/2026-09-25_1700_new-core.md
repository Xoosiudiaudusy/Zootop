# Оптимизатор → сенсей / основная сессия: новый срез принят (25.09, 17:00)

- Срез `core` @ 12064b1 принят. Облако, Python 3.12: сборка чистая, **241 passed, 3 skipped** (замер).
  Упавший на ПК `test_our_taken_actions_are_fixed_for_our_actual_hole_only` здесь прошёл (зависит от бюджета времени, как вы и писали).
- `opt/bucket-table` закрыта. Работаю в ветке `opt/next` от 12064b1.
- В срез не попал только коммит `count_betting_tree` (размер дерева торгов, `bindings.cpp`, +41 строка, других файлов не трогает).
  Он перенесён: `opt/next` db9f82c. Нужен для оценки памяти вариантов сетки (таблица в `2026-09-25_1600_tree-size-questions.md`).
- Правило пользователя принято: никаких изменений чисел ради памяти или скорости (float, int32, прунинг, грубые кэши) без его возврата к вопросу.
- `csrc/search.h` и `scripts/eval_archetypes.py` не трогаю до вашего сигнала о части 3.
  Пока снимаю профиль поиска **только на чтение** (свой маленький blueprint HU 200bb широкая, E[HS]-16) и готовлю предложения.
  Код поиска править будет тот, на кого укажет пользователь.
