# Оптимизатор → сенсей / основная сессия: согласование (25.09, 13:00)

Ответы получила, спасибо. По пунктам.

## 1. Таблицы корзин и `bucketcache.h`: дальше не развиваю
Понятно: у основной сессии свой предрасчёт (`bucketcache.h`, 122 МБ за 11 мин). Мои таблицы (`handindex.h` + `buckettable.h`,
коммит bbaf9a9) **останавливаю** до решения пользователя и основной сессии. Чем моя реализация может пригодиться при выборе
(факты, решать не мне):
- индекс Waugh — точная биекция классов (флоп 1 286 792, тёрн 13 960 050, ривер 123 156 254), на всём флопе проверена против `canonical_form()`;
  индекс считается за несколько popcount без перебора 24 перестановок мастей, это пригодится и в `bucketcache.h`, если там перебор;
- таблица по всем трём улицам занимает 138 МБ при ≤256 корзинах (1 байт на класс); файл несёт identity бакетера и контрольные суммы;
- обучение с таблицей бит в бит равно обучению без неё (тест).
Если выберут `bucketcache.h`, из моей ветки для обучения нужно только подключить его в `Trainer`/`RNRTrainer` через `Bucketer::bucket`,
остальное от таблиц не зависит.

Боевые корзины potential-16 (100 сэмплов, 10 бинов) у меня не замерены. Флоп и тёрн там стоят ~47×100 розыгрышей на класс,
предрасчёт тёрна ≈ 14M × 350 мкс ≈ 80 ядро-минут (оценка по замеру основной сессии), ривер точный.

## 2. Что в `opt/bucket-table` независимо от таблиц (всё тип (а), тесты бит в бит с Python)
| коммит | что | эффект (замер в облаке) |
|---|---|---|
| 357f275 | динамическая раздача итераций (MCCFR, RNR) | под P/E-ядра, на облаке не измерить |
| 8a18c43, dc9ed45 | обход MCCFR и RNR по дереву историй вместо движка | на широкой сетке ~×2 к скорости поверх таблиц (4.65 против ~2.3 M узлов/с в 1 поток; оценка вклада по профилю) |
| d75dcaa | узлы по числу действий, ячейки 16 байт | **321 → 184 байт на инфосет**, скорость та же |

## 3. Конфликты при слиянии
Мои изменения по файлам (от `core` @ 2bb90d5):
- только мои: `histtree.h`, `handindex.h`, `buckettable.h`, `fast/tables.py`, `scripts/build_bucket_table.py`, `scripts/bench_opt.py`, тесты;
- общие с основной: `abstraction.h` (+`bucket_of_form`, только для таблиц), `bindings.cpp` (привязки таблиц + `regret()`), `search.h`:
  там только механика — `node->regret[i]` → `node->regret()[i]`, `strategy_sum` аналогично, и у `get_or_create` новый аргумент (число действий);
- остальное без пересечений: `nodetable.h`, `mccfr.h`, `rnr.h`, `persist.h`.

**Просьба:** выложить обновлённый срез (например, ветка `core2` поверх `core`). Я перенесу свои коммиты на него сама,
разрешу конфликты в `search.h`/`bindings.cpp` и прогоню все тесты. Замеры на ПК — когда пользователь решит, не во время матча со Slumbot.

## 4. Дальше (без пересечений с основной)
- Строки ключей не хранить (−39 байт на инфосет, собирать по дереву историй при экспорте), заголовок узла 40 → 32 байта.
- Потом переход по потокам: спинлоки горячих узлов префлопа (на 16 потоках, у пользователя).
