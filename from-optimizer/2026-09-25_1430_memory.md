# Оптимизатор → сенсей / основная сессия: память (25.09, 14:30)

Ветка `opt/bucket-table`, коммиты d75dcaa и 447b105. Всё тип (а): однопоточно бит в бит с Python, форматы файлов не менялись,
229 тестов passed, 3 skipped (замер).

## Байты на инфосет (замер в облаке, HU 100bb, 4 улицы, широкая сетка, 276 тыс. инфосетов, после 2M итераций, учтено всё)
| | `core` | после d75dcaa | после 447b105 |
|---|---|---|---|
| ячейки хеш-таблицы | 122 | 61 | 30 |
| узлы (сожаления, суммы, заголовок) | 161 | 85 | 83 |
| строки ключей | 39 | 39 | 0 |
| дерево историй | не было | 113 (не входит в 184) | 28.5 |
| **итого** | **321** | 184 (без дерева) | **142** (с деревом) |

Что сделано:
1. Узел выделяется ровно под свои `n` действий: заголовок 40 байт + 16n (в среднем 2.6 действия).
   Доступ через `regret()` и `strategy_sum()`. Локальный `Node` по-прежнему вмещает 8 действий.
2. Ячейка таблицы `{k1, node}` — 16 байт. Вторая половина ключа (k2) и строка ключа лежат в узле.
3. Строки ключей узлов из обхода не хранятся: узел ссылается на свой узел дерева и корзину, `key_string()` собирает строку по требованию.
   Узлы из импорта и по строке хранят копию, как раньше.
4. Дерево историй компактное: терминалы хранят только вклады и фолды, у узлов решения дочерние указатели и кэш `Node*` лежат inline,
   точного размера. Точное действие восстанавливается из сетки по id (`BetGrid::action_from_id`, при создании узла проверяется совпадение с `abstract_actions`).
5. Таблицы тренеров растут при заполнении 3/4 вместо 1/2 (`set_dense`): в таблицу ходят только при промахе кэша указателей.
   Таблицы поиска оставлены с 1/2.
6. `table_stats()` показывает и дерево (`tree_nodes`, `tree_bytes`).

Скорость не пострадала, стала выше (замер, облако): 4.65 → 5.70 M узлов/с в 1 поток, 17.2 → 19.9 на 4 потоках.

## Что это значит для 64 и 256 корзин (оценка, не замер)
Узлов дерева не больше, чем раньше, а кэш указателей растёт как 8 байт × корзины на историю, то есть ~8 байт на инфосет.
HU 200bb широкая потенциальная-16: 3.04M инфосетов (замер сенсея), при 256 корзинах ~16× → ~50M инфосетов.
Раньше ~15 ГБ (оценка по 300 байтам), теперь ~6.5 ГБ (оценка по ~130 байтам).
Для 6-max масштаб другой, нужен замер числа историй.

## Следующее, что можно сделать с памятью (предложения)
- (а) visits в uint32 и заголовок 40 → 32 байта: −8 байт.
- (б) сожаления и суммы во float: 16n → 8n, минус ~21 байт на инфосет, итог ~110 байт. Меняет математику, нужна приёмка по кривой пуш/фолда.
- (б) сожаления int32 с масштабом и прунингом, как в Pluribus: ещё меньше. Тоже изменение алгоритма.
